// import { checkDatabase } from './models/db';

import { app } from './app';

let exp: any;

// checkDatabase().then((result) => {
//     if (result === true) {
//         const server = app.listen(8000);
//         exp = server;
//     }
// });

export default () => exp;


